# 核心技术设计开发文档

## Windows 网络操作

采用**混合实现**：

- 优先通过 PowerShell 的 `NetTCPIP` 模块读取和修改：
  - `Get-NetAdapter`
  - `Get-NetIPConfiguration`
  - `Get-NetRoute`
  - `Get-NetIPInterface`
  - `New-NetRoute`
  - `Set-NetRoute`
  - `Remove-NetRoute`
- 不解析面向用户显示的表格文本，统一转成结构化对象或 JSON。
- `route.exe` 只用于兼容验证和受控命令窗口，不作为页面功能的主要数据源。
- IPv6 重置使用 `Disable-NetAdapterBinding`、`Enable-NetAdapterBinding`。
- 路由命中结果由程序按**最长前缀优先、路由跃点与接口跃点综合判断**计算，同时提供 Windows 原生查询结果进行交叉验证。
- 所有修改操作执行后，必须重新读取 Windows 实际路由表，不能只更新页面内存数据。

## 项目模块

1. **应用基础模块**
   - WPF、MVVM、依赖注入、日志、管理员权限、全局异常处理。
2. **网卡管理模块**
   - 物理/虚拟网卡识别。
   - IPv4/IPv6 地址、DNS、网关、状态和接口索引展示。
   - 允许 DNS、网关为空。
3. **路由只读模块**
   - IPv4、IPv6 分页展示。
   - 搜索、筛选、刷新。
   - 标记临时/永久、系统/用户可操作状态。
4. **路由匹配模块**
   - 输入目标 IP，显示所有候选路由和最终命中路由。
   - 显示前缀长度、下一跳、接口、路由跃点、接口跃点和判断理由。
5. **IPv4 路由管理模块**
   - 单条/批量新增、修改和删除。
   - CIDR 校验。
   - 默认临时生效。
   - 支持有网关和无网关的 on-link 路由。
   - 修改采用安全的“校验—执行—重新读取—验证”流程。
6. **网卡跃点模块**
   - IPv4 接口跃点查看和设置。
   - 自动跃点与手动跃点切换。
   - 修改前后对比。
7. **备份恢复模块**
   - 备份全部 IPv4 路由及必要网卡信息。
   - JSON 格式、版本号、时间、主机信息和校验摘要。
   - 恢复时展示差异，只处理用户勾选项。
   - 不默认删除当前系统中额外存在的路由。
8. **IPv6 重置模块**
   - 选择网卡后重置 `ms_tcpip6` 绑定。
   - 显示风险提示、执行进度和结果。
   - 适配网卡名称中的空格和特殊字符。
9. **网络测试模块**
   - 路由命中测试。
   - 可补充 Ping、Tracert、DNS 解析和基本连通性结果。
10. **受控网络命令模块**
    - 严格白名单和参数解析。
    - 禁止任意 CMD/PowerShell 执行及命令连接符。
    - 保存执行记录。

# 分阶段开发规则

Codex 每次只能完成一个小模块：

```mermaid
flowchart LR
    A["规划当前模块"] --> B["编写测试"]
    B --> C["实现模块"]
    C --> D["构建与自动测试"]
    D --> E["交付人工测试步骤"]
    E --> F{"你是否验收通过"}
    F -- "否" --> C
    F -- "是" --> G["允许进入下一模块"]
```

每个阶段结束后，Codex必须停止，等待你明确回复“验收通过，继续下一模块”，不能自行推进。

如果大哥确认这套模块边界和开发顺序，我下一条就生成**完整可复制的 Codex 主提示词**；提示词中会包含需求、技术栈、目录结构、安全约束、验收标准，以及强制暂停规则。你回复“确认”即可。