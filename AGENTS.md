# NetRoute Studio 项目协作规则

## 语言与代码保护

- 所有回复、说明和 Git 提交说明使用中文。
- 修改前先检查工作区，保留用户已有修改，不回滚无关内容。
- 用户要求开发或修复时，应先说明方案并等待用户明确确认后再修改。

## 每次代码修改后的必做事项

- 按照 `docs/开发收尾清单.md` 完成版本、文档、构建和测试检查。
- 功能、修复或发布配置发生变化时递增补丁版本；不得只修改单一位置的版本号。
- 更新 `CHANGELOG.md`，说明用户可感知的变化及日期。
- 执行 Release 构建、稳定测试和 `git diff --check`。
- 最终回复必须说明版本、构建结果、测试结果以及修改是否已提交。
- 未经用户明确要求，不创建 Git 提交、标签，不推送远端。

## 发布约束

- 稳定测试默认排除依赖真实网卡、DNS、IPv6 或公网环境的集成测试。
- 安装程序相关修改需要检查 Inno Setup 脚本和 GitHub Actions 发布流程。
- Git 标签必须与应用版本一致，格式为 `vX.Y.Z`。
